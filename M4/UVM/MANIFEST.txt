Run:        M4/UVM post-fix async FIFO verification
Host:       PSU ECE farm via katnemo
Questa:     2021.3_1 (/pkgs/mentor/questa/2021.3_1/questasim/bin)
License:    1717@mentor-lic.cecs.pdx.edu

Baseline RED:
  Remote dir: ~/claude-runs/2026-05-25T22-11-34Z_m4-uvm-baseline-red
  Command: timeout 180s vsim -c -do 'do run.do; quit -f'
  Result: run.do failed immediately on a fresh farm directory because
          bare `vdel -all` could not access missing work/.

Fixes applied:
  RTL:
    D-1. Routed synchronizer_r2w_inst to the write clock/reset domain:
         .clk(wclk), .rst_n(wrst).
    D-2. Routed synchronizer_w2r_inst to the read clock/reset domain:
         .clk(rclk), .rst_n(rrst).
    D-3. Computed wHalfFull/rHalfEmpty from next local pointer
         occupancy so they assert at the same accepted edge as the
         threshold-crossing write/read.

  UVM testbench:
    T-1. Drivers now drive at negedge, before the DUT sampling edge.
    T-2. Monitors sample request/pre-edge flag state after negedge and
         publish only accepted transfers after the active edge.
    T-3. Scoreboard now compares DUT rData against queued expected
         wData, raises uvm_error on mismatch, and prints a final
         PASS/FAIL summary.
    T-4. Test and sequence transaction counts use shared package
         parameters.

  Run / coverage:
    C-1. run.do guards vdel for fresh farm runs.
    C-2. run.do uses one vopt/vsim flow with DUT-only code coverage:
         +cover=sbfec+asynchronous_fifo(rtl).
    C-3. M4 functional coverage now targets reachable FIFO behavior.
    C-4. Added farm VCD dump and reproducible waveform renderers.

Full M4/UVM farm run:
  Remote dir: ~/claude-runs/2026-05-25T22-36-16Z_m4-uvm-green-final2
  Start UTC: 2026-05-25T22-36-37Z
  End UTC:   2026-05-25T22-36-57Z
  Command:   vsim -c -do 'do run.do; quit -f'
  Transcript:
    All vlog phases: Errors 0, Warnings 0
    vopt:            Errors 0, Warnings 0
    vcover reports:  Errors 0, Warnings 0
    Final footer:    Errors 0, Warnings 0
  Scoreboard:
    Writes observed       : 1267
    Reads  observed       : 1267
    Residual expected_q   : 0
    Mismatches / errors   : 0
    Verdict: *** PASSED ***
  UVM:
    UVM_WARNING : 0
    UVM_ERROR   : 0
    UVM_FATAL   : 0
  Coverage:
    DUT filtered instance coverage: 100.00%
    TOTAL COVERGROUP COVERAGE:     100.00%
    Covergroup bins:               26/26

Directed flag-threshold farm run:
  Remote dir: ~/claude-runs/2026-05-25T22-26-45Z_m4-flag-threshold-green
  Start UTC: 2026-05-25T22-26-47Z
  End UTC:   2026-05-25T22-26-51Z
  Command:   vsim -c -do 'do run_flags.do; quit -f'
  Transcript:
    async_fifo.sv compile:        Errors 0, Warnings 0
    flag_threshold_tb.sv compile: Errors 0, Warnings 0
    vopt:                         Errors 0, Warnings 0
    final footer:                 Errors 0, Warnings 0
  Evidence prints:
    [OK half_full_immediate] occ=32 wHalfFull=1
    [OK full_immediate] occ=64 after write #64, wFull=1
    [OK overflow_blocked] full write attempt rejected: occ=64 accepted_writes=64 wFull=1
    [OK half_empty_immediate] occ=32 rHalfEmpty=1
    [OK empty_immediate] occ=0 after read #64, rEmpty=1
    [SUMMARY] half_full_lag_seen=0 half_empty_lag_seen=0 full_ok_seen=1 overflow_blocked_seen=1 empty_ok_seen=1

Artifacts:
  transcript_farm.txt
  waveform_samples.csv
  waveforms.svg/.png
  flag_threshold_transcript_farm.txt
  flag_debug_samples.csv
  flag_debug_waveforms.svg/.png
  flag_debug_write_thresholds.svg/.png
  flag_debug_read_thresholds.svg/.png
  make_artifacts.py
  make_flag_debug_waveforms.py
  design.md
  MANIFEST.txt

Raw simulator artifacts intentionally gitignored:
  dump.vcd
  flag_threshold.vcd
  async_fifo.ucdb

Verdict:
  PASS. M4/UVM now compiles cleanly on the farm, self-checks with a real
  data scoreboard, reaches zero UVM warnings/errors/fatals, reports
  100% DUT filtered code coverage and 100% M4 covergroup coverage, and
  proves immediate half/full/empty flag behavior with a directed farm
  waveform run.
